"""
Infrastructure adapters for AnimeMangaDB.

The Infrastructure Layer contains implementations that interact with
external systems such as databases, HTTP services, files, and other
framework-dependent resources.

Infrastructure code may depend on the Domain Layer.

The Domain Layer must never depend on this package.
"""